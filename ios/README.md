# FreeCam iOS

This folder will contain the iPhone sender.

Planned first implementation:
- Swift
- AVFoundation capture
- VideoToolbox H.264 encoding
- TCP control client
- UDP video sender

The first milestone only needs to stream the camera to a known Windows IP address.
